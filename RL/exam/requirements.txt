# Reinforcement Learning Project Requirements
# Для установки: python -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# Core RL libraries
stable-baselines3==2.2.1
gymnasium==0.29.1
# Примечание: Для установки box2d-py требуется swig
# macOS: brew install swig
# Linux: sudo apt-get install swig (Ubuntu) или sudo dnf install swig (Fedora)
# После установки swig выполните: pip install box2d-py
# Или используйте предкомпилированную версию: pip install box2d-py==2.3.10
box2d-py>=2.3.8
pygame>=2.1.3  # Требуется для LunarLander-v2 и других Box2D сред

# Visualization and analysis
matplotlib==3.8.2
seaborn==0.13.0
numpy==1.26.4
pandas==2.1.4

# Video recording for animations
imageio==2.33.1
imageio-ffmpeg==0.4.9
moviepy>=1.0.3  # Требуется для записи видео в Gymnasium

# Utilities
tqdm==4.66.1
rich>=13.0.0  # Для progress bar в stable-baselines3
pyyaml==6.0.1
tensorboard>=2.15.0  # Для логирования обучения (опционально, но рекомендуется)

